@page "/users/create"
@using ApiContracts.DTO.User
@using BlazorApp.Services
@inject IUserService UserService
@inject NavigationManager Navigation

<PageTitle>Create user</PageTitle>
<h3 class="text-center text-primary">Create USER:</h3>

<EditForm Model="@user" OnValidSubmit="CreateUser">
    <DataAnnotationsValidator />
    <div class="mb-3">
        <label class="form-label text-primary">Username: </label>
        <InputText @bind-Value="user.Username"  placeholder="username to create" class="form-control" />
        <ValidationMessage For="@(() => user.Username)" />
    </div>
    <div class="mb-3">
        <label class="form-label text-primary">Password: </label>
        <InputText type="password" @bind-Value="user.Password"   placeholder="password" class="form-control" />
        <ValidationMessage For="@(() => user.Password)" />
    </div>
    <div class="mb-3">
        @if (user.Username.Length > 3 && user.Password.Length > 1)
        {
            <button type="submit" class="btn btn-primary">Create User!</button>
        }
        <button type="button" class="btn btn-secondary" @onclick="Back">Go back</button>
    </div>
</EditForm>
@if (user.Username.Length < 3)
{
    <div class="alert alert-danger"> Username must be longer than 3 characters: @user.Username.Length</div>
}else if (user.Password.Length < 1)
{
    <div  class="alert alert-danger">Password must be longer than 1 character: @user.Password.Length </div>
}




@code {
    private UserCreateDto user = new() { Username = string.Empty, Password = string.Empty };
   

    
    
    private async Task CreateUser()
    {
        try
        {
            
            await UserService.AddUserAsync(user);
            Navigation.NavigateTo("/users");
        }
        catch (Exception ex)
        {
           Console.Write(ex.Message);
        }
    }

    private void Back()
    {
        Navigation.NavigateTo("/users");
    }
}